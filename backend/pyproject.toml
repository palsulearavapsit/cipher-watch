[project]
name = "sentinel"
version = "0.1.0"
description = "SentinelAI — source-agnostic real-time anomaly detection"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
